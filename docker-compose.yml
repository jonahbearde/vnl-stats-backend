version: '3'

services:
  express-ts-starter:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules


volumes:
  node_modules: